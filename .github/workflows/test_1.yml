
name: Hola Mundo1

on: [push]

jobs:
  holaone:
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo2
        run: echo "¡Hola, mundo!"
      - name: touch
        run: touch file.txt
      - name: write file
        run: echo "prueba actions"  >> file.txt
      - name: read file
        run: cat file.txt

  other:
    runs-on: ubuntu-latest
    steps:
      - name: LS
        run: ls -al

